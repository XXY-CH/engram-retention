# Utilities

This folder is reserved for shared helpers that are used across multiple source
modules.

Keep utilities narrow and dependency-light. If a helper is specific to one layer,
model, or experiment, keep it next to that code instead of placing it here.
