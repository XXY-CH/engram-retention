# Analysis Scripts

This folder holds executable plotting scripts.

## Files

- `generate_academic_plots.py` - Creates first-pass paper-style figures for the
  synthetic ablation story.
- `generate_advanced_plots.py` - Creates refined figure variants and depth-route
  visualizations.

Run these scripts from the repository root so relative output paths resolve
under `analysis/`.
